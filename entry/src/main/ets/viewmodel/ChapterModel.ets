export class ChapterModel {
  id: string = '';            // 章节id
  course_id: string = '';      // 所属课程id
  title: string = '';         // 章节标题
  chapter_order: number = -1;  // 章节顺序
  // isExpanded: boolean = false;// 是否展开
}