export class ChapterModel {
  id: string = '';            // 章节id
  courseId: string = '';      // 所属课程id
  title: string = '';         // 章节标题
  chapterOrder: number = -1;  // 章节顺序
  // isExpanded: boolean = false;// 是否展开
}